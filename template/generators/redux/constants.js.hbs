import {actionTypes} from '../actionTypes'
import {IActionTypes} from '../types'

const {{constantCase name}}_ACTIONS: IActionTypes = actionTypes('{{constantCase name}}_ACTIONS')

export const {{constantCase name}}_CONSTANTS_ACTIONS = {
  {{constantCase name}}_ACTIONS,
}
