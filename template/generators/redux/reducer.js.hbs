import {createSlice} from '@reduxjs/toolkit'
import { {{constantCase name}}_CONSTANTS_ACTIONS } from '../constants/{{camelCase name}}'
import {I{{properCase name}}, I{{properCase name}}Actions} from '../../constants/interface/redux/{{properCase name}}Interface'

export const initialState: I{{properCase name}} = {}

export const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState,
  reducers: {
    [{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.HANDLER]: () => {},
    [{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.SUCCESS]: () => {},
    [{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.FAILURE]: () => {},
  },
  extraReducers: builder => {},
})

export const {{camelCase name}}Actions: I{{properCase name}}Actions = {
  ...{{camelCase name}}Slice.actions,
  {{camelCase name}}Handle: {{camelCase name}}Slice.actions[{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.HANDLER],
  {{camelCase name}}Success: {{camelCase name}}Slice.actions[{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.SUCCESS],
  {{camelCase name}}Failure: {{camelCase name}}Slice.actions[{{constantCase name}}_CONSTANTS_ACTIONS.{{constantCase name}}_ACTIONS.FAILURE],
}

export default {{camelCase name}}Slice.reducer
